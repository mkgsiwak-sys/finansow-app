# Usuń lub zakomentuj linie dotyczące bazy H2, jeśli istnieją
# np. #spring.h2.console.enabled=true
# np. #spring.datasource.url=jdbc:h2:mem:testdb

# Połączenie z PostgreSQL na Supabase
spring.datasource.url=jdbc:postgresql://ymhbdmuvdlvgnhmtfbcb.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=rTYxuTfy6yTqwuBK # <-- TWOJE HASŁO ZOSTAŁO WSTAWIONE

# Konfiguracja Hibernate (dla PostgreSQL)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Ustawienie update pozwala Hibernate automatycznie tworzyć/aktualizować tabele w bazie
spring.jpa.hibernate.ddl-auto=update

# Opcjonalnie: Konfiguracja portu serwera (domyślnie 8080)
# server.port=8080